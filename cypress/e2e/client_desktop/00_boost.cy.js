import urls from "../../support/client/urls";
describe('boostCasino Desktop Test Suite', () => {
    beforeEach(() => {
        cy.handleCookieConsent();
        urls.visitBoostHome();
    });

    it('test-xxx', () => {
    
    });
});